export const Body = ({ number, arr }) => (
    <div>
        Edit <code>src/App.js</code> and save to reload.
        <div>{number}</div>
        <span>{arr[0]}</span>
    </div>
)

//export default Body


